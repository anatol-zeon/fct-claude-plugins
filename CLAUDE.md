# fct-tg-bot — project rules for Claude Code

Этот файл читается каждой сессией. Уточняет глобальный `~/.claude/CLAUDE.md` для этого конкретного проекта.

## 1. Что это

<одно-два предложения: назначение продукта, целевая аудитория, стадия>

## 2. Стек

- **Языки**: <Python 3.12 / Node 22 / ...>
- **Фреймворки**: <FastAPI / Next.js 15 / ...>
- **База**: <Postgres 16 / SQLite / ...>
- **Очереди/брокеры**: <Redis / nats / NA>
- **Инфра**: <Docker Compose / NA пока>

## 3. Как запустить локально

```bash
# bootstrap
./scripts/bootstrap.sh

# run all services
./scripts/dev.sh
```

## 4. Команды тестов

```bash
# все сервисы
./scripts/test.sh

# конкретный сервис
cd services/<name> && <pytest|npm test>
```

## 5. Структура

Следует общему скелету (`~/.claude/templates/project-skeleton/`). Специфика проекта:

- **Сервисы** (`services/`): <перечислить по мере появления>
- **Внешние API** (см. `docs/apis.md`): <TBD>

## 6. Сервисы этого проекта

| Сервис | Назначение | Порт | Статус |
|---|---|---|---|
| _(пусто)_ | | | |

## 7. Проектные правила (override глобальных)

- <любые уточнения к глобальным правилам>

## 8. Текущий фокус

<что сейчас в работе — обновлять через `/revise-claude-md` в конце сессии>
